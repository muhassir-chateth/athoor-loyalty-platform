/**
 * `athoor-portal-fragrance.js` — the Fragrance Profile section bundle's entry
 * point.
 *
 * TASK 7.1 OWNS THIS FILE'S EXISTENCE; TASK 24.2 OWNS ITS BEHAVIOUR.
 * See `overview.ts` for why every section entry is a bare registration at this
 * stage. The preference dimensions, the single-valued intensity rule and the
 * declared/inferred provenance separation are task 24.2.
 */
import { registerSection } from "./registration.js";

registerSection("fragrance", () => {
  // Task 24.2.
});
