/**
 * `athoor-portal-settings.js` — the Settings section bundle's entry point.
 *
 * TASK 7.1 OWNS THIS FILE'S EXISTENCE; TASKS 26.2–26.3 OWN ITS BEHAVIOUR.
 * See `overview.ts` for why every section entry is a bare registration at this
 * stage. The communication preferences are task 26.2; the data export and the
 * erasure-request flow are task 26.3.
 */
import { registerSection } from "./registration.js";

registerSection("settings", () => {
  // Tasks 26.2–26.3.
});
