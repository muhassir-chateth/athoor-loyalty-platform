/**
 * `athoor-portal-wishlist.js` — the Wishlist section bundle's entry point.
 *
 * TASK 7.1 OWNS THIS FILE'S EXISTENCE; TASK 21.2 OWNS ITS BEHAVIOUR.
 * See `overview.ts` for why every section entry is a bare registration at this
 * stage. Reading the set, enriching it from the catalogue, removal, add-to-bag,
 * the 500-item `409` state and per-row degradation are tasks 21.2 and 21.3, over
 * the N5 contract task 9.1 provides.
 */
import { registerSection } from "./registration.js";

registerSection("wishlist", () => {
  // Task 21.2.
});
